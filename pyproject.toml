[project]
name = "netsniff"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "psutil>=7.0.0",
    "pyinstaller>=6.16.0",
    "scapy>=2.6.1",
]
